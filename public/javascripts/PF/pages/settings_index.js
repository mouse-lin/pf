Pf.settings.homeIndex = { 
    panel: function(){ 
        var panel = new Ext.form.FormPanel({ 
            autoScroll : true,
            title: "nihao",
            frame: true,
        });
        return panel;
    } 
}
